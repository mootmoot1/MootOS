# MootOS Version 0.1 Requirements

**Last reviewed:** July 31, 2026

## Purpose

Version 0.1 proves that MootOS can function as a reliable personal AI foundation.

Primary question:

> Can Moot securely use one ongoing assistant from his phone, keep conversations and important information across deployments, deliberately save useful facts, organize them into projects, and continue improving the system without rebuilding it?

## Status legend

- ✅ **Verified** — implemented and manually or automatically verified
- 🟡 **Partial** — useful foundation exists, but the complete workflow is unfinished
- ⬜ **Not implemented** — planned but not built
- ➡️ **Deferred** — intentionally moved later

## Current foundation

Implemented or verified:

- Private Railway phone deployment
- Persistent conversations on the `/data` SQLite volume
- Mobile chat interface
- Project organization
- Memory CRUD APIs
- Global and project memory context
- Hardened SQLite connections and versioned migrations
- Railway auth that fails closed
- Replaceable model-provider boundary and OpenAI provider
- Explicit `remember` and `save this` commands on the current draft branch
- Database write before memory confirmation
- Cross-chat recall tests

Primary incomplete Version 0.1 work:

- Production verification of chat memory commands
- Memory review and correction controls
- Safe forget and update commands
- Better retrieval and duplicate handling
- Conversation behavior refinement
- Basic settings and readable activity logging
- Automatic backup and tested restore

---

## Deliverable 1 — Natural conversation

**Status:** 🟡 Partial

Implemented:

- Recent conversation history supplied to the model
- Conversations continue across sessions
- Base MootOS identity instructions
- Relevant memories can influence responses
- Ordinary questions such as `Do you remember ...?` remain normal model requests

Still required:

- Better behavior for notes and short status statements
- More reliable reference resolution
- Clear behavior when context is insufficient
- Additional conversation regression tests

---

## Deliverable 2 — Long-term memory

**Status:** 🟡 Partial; explicit save path implemented on the draft branch

Implemented:

- Persistent `memories` table
- Create, list, retrieve, filter, and delete APIs
- Global and project-scoped model context
- Up to 20 newest relevant memories supplied to the model
- Persistence across Railway deployments
- Deterministic commands beginning with `remember`, `save this`, or `save to memory`
- Chat saves use memory type `explicit_chat`
- Explicit saves bypass the external model provider
- Confirmation occurs only after SQLite accepts the write
- End-to-end test saves in one chat and recalls in a brand-new chat

Still required:

- Railway production verification
- Better relevance than newest-first retrieval
- Source and confidence metadata
- Duplicate, conflict, and correction handling

---

## Deliverable 3 — Memory management

**Status:** 🟡 Partial

Implemented:

- Remember through explicit chat commands
- View through the memory API
- Delete through the memory API
- Global memory scope when no project is selected
- Project isolation for project memories

Still required:

- `Forget that ...` command
- `Update that ...` command
- Memory review and editing interface
- Keyword search
- Duplicate detection
- Confirmation for destructive changes

---

## Deliverable 4 — Project organization

**Status:** ✅ Verified foundation

Implemented:

- Five default projects
- Project creation and listing
- Case-insensitive duplicate protection
- Project validation
- Project-filtered lists
- Project selector in chat
- Project-scoped memory recall
- Global memories available inside project chats

Richer project workspaces are deferred to Version 0.2.

---

## Deliverable 5 — AI identity

**Status:** 🟡 Partial

Implemented:

- Base instructions identify MootOS Version 0.1
- Core purpose and honesty rules
- Rule forbidding false claims that memory was saved

Still required:

- Stronger consistency across conversation types
- Documented communication preferences
- Better awareness of unavailable capabilities

---

## Deliverable 6 — Mobile interface

**Status:** 🟡 Partial

Implemented:

- Responsive chat
- Message composer and bubbles
- Conversation history
- Project selector
- Loading and error states
- Login, logout, and Home Screen manifest
- Explicit memory commands through the normal composer

Still required:

- Memory review and management interface
- Basic settings interface
- More distinct MootOS visual design

---

## Deliverable 7 — Replaceable AI engine

**Status:** ✅ Verified foundation

Implemented:

- Provider protocol and router
- OpenAI provider
- Environment-based selection
- Fake-provider tests
- Provider-side response storage disabled
- Explicit memory saves handled internally without OpenAI

Current limitation:

- OpenAI is the only implemented external provider

---

## Deliverable 8 — Basic settings

**Status:** ⬜ Not implemented

Provider and model currently use environment variables. There is no settings page or persisted settings model.

---

## Deliverable 9 — Logging

**Status:** 🟡 Partial

Implemented:

- Timestamped conversations and messages
- Provider and model metadata
- Internal memory confirmations marked `mootos` and `memory-command-v1`
- Railway logs and migration history

Still required:

- Readable activity log
- Memory change history
- Privacy-aware logging rules
- Clear error identifiers

---

## Deliverable 10 — Tool framework

**Status:** ⬜ Not implemented

There is no runtime tool registry, permission system, execution history, or approval flow. External ChatGPT connectors are not built-in MootOS tools.

---

## Foundation requirements

### Secure private deployment

**Status:** ✅ Verified foundation

- Password login
- Signed HTTP-only sessions
- Secure Railway cookies
- Protected application and APIs
- Minimal public health endpoint
- Railway startup fails when private auth is missing
- Public Railway startup requires an explicit override

Still required: login rate limiting and stronger password management.

### Persistent production storage

**Status:** ✅ Hardened foundation; automatic recovery incomplete

- Railway volume at `/data`
- Central database connection layer
- Foreign keys, WAL, busy timeout, and `NORMAL` synchronous mode
- Numbered migrations and schema verification
- Existing-data preservation tests
- Concurrent-write tests
- Post-hardening conversation persistence verified by Moot

Still required: automatic backups, off-volume encrypted copies, and restore testing.

### Understandable development process

**Status:** ✅ Verified foundation

- Focused branches and PRs
- Automated tests
- Plain-language review
- Explicit merge approval
- Documentation updated with behavior changes

---

## Definition of done

Version 0.1 is complete when:

> Moot can securely open MootOS on his phone, hold a natural ongoing conversation, deliberately save important information, retrieve it later, review and correct memories, remove outdated memories safely, organize information into projects, and operate through deployments without losing data or depending on undocumented steps.

## Recommended completion order

1. Review, merge, and verify explicit chat memory saves
2. Memory review and correction interface
3. Safe forget and update commands
4. Keyword search and duplicate handling
5. Conversation behavior refinement
6. Curated Moot bootstrap profile import
7. Basic settings and activity logging
8. Version 0.1 release review

Reliability, clarity, privacy, and user control are more important than feature count.
