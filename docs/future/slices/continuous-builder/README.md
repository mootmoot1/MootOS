# Continuous Builder Seed Slice Specs

These 20 files capture the product/architecture decisions from the September 2, 2026 design session. They are **seed specs**, not immediate implementation instructions. Before each is built, reconcile it against current `main`, current ADRs, the active V0.4 checkpoint, exact files/interfaces, and current test contracts.

This protects the project from fake precision while preserving the intended behavior.
